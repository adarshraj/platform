# CrowdSec bouncer API key
# After first run, generate with:
#   docker exec crowdsec cscli bouncers add traefik-bouncer
# Copy the generated key here.
CROWDSEC_BOUNCER_API_KEY=your-bouncer-api-key-here
