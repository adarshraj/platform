# Copy to .env and fill in values before running docker compose up -d

# Database password — generate with: openssl rand -hex 32
UMAMI_DB_PASSWORD=

# App secret — used to sign sessions, generate with: openssl rand -hex 32
UMAMI_APP_SECRET=
